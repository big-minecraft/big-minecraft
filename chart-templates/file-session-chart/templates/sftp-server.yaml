apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.sftp.podName }}
  labels:
    app: {{ .Values.sftp.podName }}
spec:
  securityContext:
    fsGroup: 1001
  restartPolicy: Never
  containers:
    - name: sftp
      image: atmoz/sftp:latest
      env:
        - name: SFTP_USERS
          value: "{{ .Values.sftp.username }}:{{ .Values.sftp.password }}:1001:1001:/home/{{ .Values.sftp.username }}"
      ports:
        - containerPort: {{ .Values.sftp.port }}
      volumeMounts:
        - name: pvc-data
          mountPath: /home/{{ .Values.sftp.username }}
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
  volumes:
    - name: pvc-data
      persistentVolumeClaim:
        claimName: {{ .Values.sftp.pvcName }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.sftp.podName }}-service
spec:
  type: NodePort
  selector:
    app: {{ .Values.sftp.podName }}
  ports:
    - port: 22
      targetPort: 22
      nodePort: {{ .Values.sftp.nodePort }}

