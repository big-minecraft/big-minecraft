FROM openjdk:21-jdk-slim

WORKDIR /mnt/local/minestom

ENV EULA=TRUE
ENV VERSION=1.20.2
ENV SERVER_PORT=25565

EXPOSE $SERVER_PORT

# Directly run the server.jar file
CMD ["java", "-jar", "server.jar", "nogui"]