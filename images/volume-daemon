FROM python:3.11-buster

WORKDIR /app

COPY ../internal-scripts/redis_listener.py .

RUN apt-get update && \
    apt-get install -y nfs-common rsync && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

RUN pip install redis

CMD ["python", "redis_listener.py"]
